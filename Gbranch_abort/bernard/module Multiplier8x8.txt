module Multiplier8x8 (
    input  [7:0] a,        // First operand (e.g., dataA)
    input  [7:0] b,        // Second operand (e.g., dataB)
    output [15:0] product  // 16-bit product
);
    assign product = a * b;
endmodule
